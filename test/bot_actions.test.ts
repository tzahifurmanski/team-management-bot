// TODO: Write the tests, they are just stubs ATM
test("app_mention - invalid command", () => {
  expect(2).toBe(2);
});

test("app_mention - compliment command", () => {
  expect(2).toBe(2);
});

test("app_mention - introduce yourself command", () => {
  expect(2).toBe(2);
});
